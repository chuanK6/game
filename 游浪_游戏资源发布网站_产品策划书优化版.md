# 《游浪》游戏资源发布网站  
产品策划书（优化版）

版本：V1.0 ｜ 定位：个人/小型游戏资源发布平台

**说明：** 本版本以原始策划内容为基础进行结构化整理。原稿中已确定的功能、技术栈和会员价格予以保留；新增内容以“优化建议/后续规划”形式补充，便于后续开发时区分 MVP 与扩展功能。

# 一、项目概述

**项目名称：**游浪（暂定名）

**项目定位：**一个以 PC 游戏资源展示、分类检索和下载为核心的游戏资源发布网站。

**核心目标：**建立清晰、易浏览、易搜索的游戏资源库，并通过免费资源与会员资源区分实现基础会员体系。

- 用户能够快速发现最近更新、按分类/标签筛选游戏，并进入详情页查看配置与资源信息。
- 游客可以浏览公开内容，但下载资源需要登录；免费资源登录后可查看下载链接，会员资源需要会员权限。
- 管理员通过后台统一管理游戏、分类、标签和用户。

# 二、目标用户与角色

| **角色** | **权限定位** | **主要能力** |
| --- | --- | --- |
| 游客  | 未登录访问者 | 浏览首页、游戏列表、搜索和游戏详情；下载区域提示登录 |
| 普通用户 | 已登录用户 | 访问免费资源下载；管理基础个人信息；查看会员状态 |
| 会员用户 | 付费用户 | 拥有会员有效期内的会员资源查看/下载权限；查看会员到期时间 |
| 管理员 | 网站管理者 | 游戏/分类/标签/用户 CRUD；启用/禁用用户；调整会员状态；处理失效资源反馈 |

# 三、技术架构

| **层级** | **技术** | **用途** |
| --- | --- | --- |
| 前端  | Vue 3 + TypeScript + Vite | 构建网站页面与交互 |
| UI  | Element Plus | 表单、分页、弹窗、后台管理组件等 |
| 状态管理 | Pinia | 登录状态、用户信息、会员状态等全局数据管理 |
| 路由  | Vue Router | 首页、游戏列表、详情、个人中心、后台等页面路由 |
| 后端  | Cloudflare Workers + Hono | 提供 REST API、权限校验、业务逻辑 |
| 数据库 | Cloudflare D1 | 用户、游戏、分类、标签、会员及反馈等数据 |
| 图片  | Cloudinary | 游戏封面上传与图片 CDN/托管 |
| 部署  | Cloudflare Pages + Workers | 前端与后端部署 |
| 代码仓库 | GitHub | 版本控制与项目协作 |
| 开发工具 | VS Code / Codex | 日常开发与 AI 辅助编程 |

# 四、网站整体信息架构

- 首页：品牌区、导航、搜索入口、最近更新游戏。
- 全部游戏：游戏列表 + 分类/标签筛选 + 搜索 + 分页。
- 游戏详情：游戏介绍、配置要求、资源权限、下载入口、失效反馈。
- 留言板：用户向管理员反馈、推荐或申请添加游戏。
- 登录/注册：用户身份认证。
- 个人中心：基本信息、密码修改、会员信息与购买入口。
- 后台管理：游戏、分类、标签、用户等管理。
- 多级导航：作为后续扩展能力，暂不作为首期 MVP 的核心功能。

# 五、前台功能设计

## 5.1 顶部导航

- 左侧：网站 Logo + “游浪”网站名称，Logo 控制在较小尺寸，避免抢占首屏空间。
- 中部：首页、全部游戏、留言板；后续预留多级导航扩展位。
- 右侧：小型搜索框 + 登录按钮；用户登录后显示头像与用户名，并作为进入个人中心的入口。

## 5.2 首页首屏

- 导航下方设置大尺寸搜索区域，作为首页核心视觉入口。
- 背景图片采用缓慢缩放效果，并自动切换下一张图片。
- 首屏与下方内容之间增加渐变过渡，避免视觉断层。
- 搜索框应支持游戏名称、关键词等基础搜索。

## 5.3 最近更新

- 采用独立卡片式布局。
- 鼠标悬浮时增加突出/浮起效果，强化交互反馈。
- 每页默认展示 20 个游戏。

| **卡片区域** | **展示内容** |
| --- | --- |
| 顶部  | 游戏封面，统一使用横向 16:9 比例；每个游戏暂定仅维护 1 张封面 |
| 中部  | 游戏分类 + 标签 |
| 底部主区域 | 游戏名称 |
| 底部信息 | 左侧显示资源发布时间（年月日）；右侧显示“免费 / 会员”标识 |

## 5.4 全部游戏 / 筛选

- 提供游戏列表、关键词搜索、分类筛选、标签筛选和分页。
- 筛选条件组合后刷新结果，保持 URL 或路由状态可追踪，便于后续分享与返回。
- 优化建议：后续可增加排序条件，例如最近更新、名称排序等，但不作为首期必做功能。

## 5.5 游戏详情页

- 展示游戏名称、封面、分类、标签、发布时间等基础信息。
- 游戏介绍：详细说明游戏内容与特色。
- 配置要求：提供最低配置和推荐配置。//只需要最低配置
- 下载资源：下载链接为网盘资源链接，可能有多个网盘。
- 权限控制：未登录用户不能看到实际资源链接，只显示“登录下载”；普通用户登录后可查看免费资源；会员资源需要会员权限。
- 资源失效反馈：在下载入口附近提供“资源失效提醒”按钮，提交给管理员处理。

# 六、账号与会员体系

## 6.1 登录状态

- 游客：可以浏览公开页面，但资源链接不可直接展示。
- 普通用户：登录后可查看免费资源链接。
- 会员用户：登录后可查看会员资源。
- 管理员：除普通用户能力外，可进入后台管理系统。

## 6.2 个人中心

- 基本信息：用户名、头像等基础信息。
- 密码修改。
- 会员信息：会员类型、状态及到期时间。
- 非会员状态：展示“1个月订阅”和“终身买断”购买卡片。

## 6.3 会员价格与升级机制

| **方案** | **价格** | **权益/状态** |
| --- | --- | --- |
| 月订阅 | ¥1  | 会员有效期按月计算，到期后恢复普通用户状态 |
| 终身买断 | ¥9.9 | 永久会员，显示“永久” |

- 付款方式：点击购买后弹出微信/支付宝收款码图片。
- 付款备注：用户必须填写自己的用户名，管理员根据付款备注识别用户并进行会员升级。
- 用户名要求：用户名唯一，避免人工升级时出现对应关系错误。
- 优化建议：后续可增加“已提交付款 / 审核中 / 已开通 / 驳回”状态，减少用户重复操作。

# 七、留言板 / 资源反馈

- 留言板用于用户向管理员反馈问题或申请添加游戏。
- 建议至少区分：添加游戏、资源失效、网站问题、其他反馈。
- 优化建议：后台增加留言状态，例如“待处理、处理中、已处理”，方便管理员追踪。

# 八、后台管理系统

## 8.1 游戏管理

- 游戏 CRUD：新增、查看、编辑、删除。
- 管理游戏名称、介绍、分类、标签、配置要求、下载地址、发布时间、资源类型（免费/会员）等信息。
- 游戏封面使用 Cloudinary 上传组件。
- 每个游戏暂定只上传 1 张封面，不维护额外详情图。

## 8.2 分类管理

- 分类新增、编辑、删除、查询。
- 分类用于“全部游戏”的筛选以及游戏卡片展示。

## 8.3 标签管理

- 标签新增、编辑、删除、查询。
- 游戏可关联多个标签，用于更细粒度的检索。

## 8.4 用户管理

- 用户 CRUD。
- 用户角色：普通用户、会员用户、管理员。
- 管理员可以启用/禁用用户。
- 管理员可以升级会员或降级为普通用户。
- 优化建议：会员信息建议不要只依赖角色，而应增加会员类型、开始时间、到期时间等字段；终身会员使用明确的永久标识。

# 九、图片上传配置

| **配置项** | **当前方案** |
| --- | --- |
| Cloud Name | wmu4lce4 |
| Unsigned Upload Preset | muri10086 |
| 上传目录 | game |
| 用途  | 游戏封面上传 |

**安全提醒：** 策划书和代码仓库中不应保存真正的私密 API Secret、数据库凭据或其他敏感密钥。当前使用的 Unsigned Upload Preset 属于前端上传方案的一部分，但仍建议后续限制允许的图片格式、大小和上传来源。

# 十、建议的数据结构

以下为根据当前功能整理出的建议数据模型，具体字段可在开发阶段调整。

| **数据表** | **核心字段建议** | **作用** |
| --- | --- | --- |
| user | id、username、password、avatar、role、status、member_type、member_expire_at、created_at | 账号、角色、会员状态 |
| game | id、name、cover_url、description、min_config、recommend_config、category_id、resource_type、download_url、publish_at、created_at、updated_at | 游戏主体信息 |
| category | id、name、sort、status | 游戏分类 |
| tag | id、name、status | 游戏标签 |
| game_tag | game_id、tag_id | 游戏与标签多对多关联 |
| feedback | id、user_id、game_id、type、content、status、admin_reply、created_at | 留言、资源失效反馈 |

# 十一、核心业务规则

- 用户名唯一：用于登录识别，也作为人工付款升级会员时的匹配依据。
- 下载权限判断：游客无权限 → 提示登录；普通用户仅可访问免费资源；会员用户可访问会员资源；管理员拥有最高权限。
- 会员状态判断：月订阅依据到期时间判断是否有效；终身会员不设置实际到期时间，显示“永久”。
- 用户禁用后：禁止登录或使用需要登录的核心功能。具体处理方式在开发阶段确定。
- 资源失效反馈：用户提交后进入后台处理列表，不直接修改资源状态。

# 十二、MVP 首期开发范围

建议第一版不要一次把所有扩展功能做完，先完成完整闭环：

- 登录 / 注册 / 退出登录。
- 首页：导航、首屏搜索、最近更新。
- 全部游戏：搜索、分类/标签筛选、分页。
- 游戏详情：介绍、配置、免费/会员权限、下载链接、失效反馈。
- 个人中心：基础信息、密码修改、会员状态。
- 会员购买：微信/支付宝收款码展示 + 用户名付款备注规则 + 管理员人工开通。
- 后台：游戏、分类、标签、用户 CRUD + 用户启用/禁用 + 会员调整。
- Cloudinary 游戏封面上传。

# 十三、第二阶段可扩展功能

- 游戏收藏 / 稍后再看。
- 浏览历史。
- 下载记录。
- 用户评分与评论。
- 更丰富的排序和筛选。
- 后台数据统计：用户数量、游戏数量、会员数量、反馈数量等。
- 留言处理状态与管理员回复。
- 多级导航和专题页。
- 自动化支付/会员开通机制（需要单独设计支付与安全方案）。

# 十四、开发时建议重点关注的问题

- 权限安全：不要仅依赖前端隐藏下载链接；后端 API 也必须验证登录身份与会员权限。
- 网盘链接保护：详情页前端不应在未授权情况下提前返回真实资源地址。
- 密码安全：数据库只保存经过安全哈希后的密码，不保存明文密码。
- 管理员权限：后台 API 必须进行角色校验，不能只靠前端路由拦截。
- 资源管理：建议为游戏增加资源状态字段，方便以后下架失效资源，而不是直接删除数据。
- 删除策略：游戏、分类、标签、用户等数据涉及关联关系时，建议优先考虑软删除或状态字段。
- 移动端适配：虽然核心场景偏 PC 网站，但首页、详情页、个人中心应保留基础移动端响应式能力。

# 十五、当前待定事项

以下内容原策划书暂未明确，建议在正式开发前确定：

- 是否开放用户注册，以及注册时需要哪些字段。 //开放，用户名和密码
- 登录方式采用用户名+密码，还是增加邮箱/验证码等方式。//暂时不用邮箱验证，后续在添加
- 游戏是否支持多个网盘链接，以及是否区分不同下载版本。//支持多个网盘，不区分版本
- “免费 / 会员”的权限是否允许管理员单独设置。//允许单独设置
- 留言板是否需要登录后才能提交。//登录才能提交
- 会员购买成功后的人工审核流程及审核状态。//添加一个我的工单在个人中心，用于查看进度
- 游戏删除后的关联分类、标签、反馈如何处理。//删除后如果只关联这一个游戏，就一并删除
- 是否需要记录下载次数、访问次数等统计数据。//不用

# 十六、页面清单

| **页面** | **核心内容** | **优先级** |
| --- | --- | --- |
| 首页  | 导航、搜索、首屏背景、最近更新 | P0  |
| 全部游戏 | 游戏列表、搜索、筛选、分页 | P0  |
| 游戏详情 | 介绍、配置、标签、资源权限、失效反馈 | P0  |
| 登录/注册 | 账号认证 | P0  |
| 个人中心 | 基本信息、密码、会员信息 | P0  |
| 留言板 | 添加游戏申请、问题反馈 | P1  |
| 后台首页 | 数据概览（建议） | P1  |
| 后台-游戏 | 游戏 CRUD、封面上传 | P0  |
| 后台-分类 | 分类 CRUD | P0  |
| 后台-标签 | 标签 CRUD | P0  |
| 后台-用户 | 用户 CRUD、启用/禁用、会员调整 | P0  |

# 十七、项目最终目标（整理版）

**第一阶段目标：**先做出一个界面简洁、数据结构清晰、前后台闭环完整的游戏资源网站。

**开发原则：**前期以“能稳定发布游戏、能稳定管理资源、权限正确、界面好用”为核心，不为了功能数量过早增加复杂度。